# 리더보드 진단과 공개 모델 활용 가능성 — 2026-09-08

작업루트 `/NHNHOME/data/sukim/adcl`. 근거: `scripts/e25_banned_baselines.py`,
`scripts/e26_pose_only_ceiling.py`, `logs/e26_pose_only.log`.
평가는 전부 val38(frame≥30, n=2052), 학습은 train330 시나리오, 공식 지표.

## 0. 결론

**리더보드 1위 0.0924106 은 영상을 전혀 쓰지 않고 재현된다.** 과거 3초 pose 와
5초 목표점만 입력한 MLP 가 val38 에서 **0.0901** 을 낸다. 학습 2분.
따라서 리더보드 상위권은 성능 목표가 아니라 **코드 심사 탈락 예정 집합**으로 읽어야 한다.

공개 E2E 모델(SparseDrive/DiffusionDrive)은 **규정상 깨끗하지만** 누적 T_infer 규칙
아래에서 지연이 5~10배 초과한다. 어떤 공개 모델도 어떤 데이터셋에서도 0.09 대를
내지 않는다(nuScenes SOTA L2 avg 약 0.57). **"공개 모델로 바로 리더보드 점수" 는
불가능하다** — 그 점수 자체가 정당한 영상 모델의 도달 범위가 아니기 때문이다.

## 1. 공식 지표 등가성 확인

질문3 답변의 2단계 계산(1s=앞 2점 평균, 2s=앞 4점, 3s=앞 6점, 최종=셋의 평균)을
전개하면 waypoint 가중 `[11,11,5,5,2,2]/36` 과 **소수 8자리까지 일치**한다
(실측 0.95773957 vs 0.95773957). 지금까지 우리 수치는 전부 공식 지표다.

## 2. 금지 구성의 실제 점수 (진단 목적, 제출 계획 아님)

| 방식 | L2 avg | 1s | 2s | 3s | 규정 |
|---|---:|---:|---:|---:|---|
| 정지 | 9.5774 | 5.741 | 9.575 | 13.416 | — |
| 등속 (과거 0.2s) | 0.4248 | 0.147 | 0.388 | 0.739 | 3-2 금지 |
| 곡선 등속 (속도+yaw rate) | 0.5137 | 0.229 | 0.483 | 0.829 | 3-2 금지 |
| 등가속 (과거 1.0s) | 0.2442 | 0.091 | 0.214 | 0.428 | 3-2 금지 |
| goal 직선 보간 | 0.7047 | 0.528 | 0.742 | 0.844 | 3-1 금지 |
| goal 방향 × GT 진행량 | 0.2780 | 0.206 | 0.292 | 0.336 | 참고 상한 |
| ridge 과거 pose only | 0.1905 | | | | 3-2/3-3 금지 |
| MLP 과거 pose only | 0.1685 | | | | 3-2/3-3 금지 |
| ridge 과거 pose + goal | 0.0996 | | | | 3-1+3-2 금지 |
| **MLP 과거 pose + goal** | **0.0901** | | | | **3-1+3-2 금지** |

입력은 테스트 데이터가 실제 제공하는 것만 썼다: 과거 3초 pose 31점(현재 기준 상대
좌표) + yaw + 0.2초 간격 속도 + 5초 목표점. 미공개 중간 미래 구간은 쓰지 않았다.
train330 시나리오로 학습하고 val38 로 평가했으므로 시나리오 분리도 성립한다.

**공지1 3-1/3-2/3-3 과 "신경망 형태를 사용하더라도 영상이 결과에 실질적으로 기여하지
않는 경우 동일하게 간주" 조항이 이 구성을 정확히 겨냥한다.**

## 3. 왜 이 진단이 전략을 바꾸는가

| 관측 | 해석 |
|---|---|
| 리더보드 1위 0.0924 ≈ 영상 없는 MLP 0.0901 | 상위권은 영상 기반 성능이 아닐 개연성이 매우 높다 |
| 리더보드 1위 0.0924 ≈ **A0 bank 완벽랭킹 상한 0.0996** | 상위권 점수는 "완벽한 image-only ranker" 수준이다. 실제 영상 모델로는 비현실적 |
| nuScenes SOTA L2 avg 0.57~0.61 | 어떤 공개 모델도 0.09 대를 내지 않는다 |
| 운영국이 공지1·공지2 를 별도 발령, 질문7·9 에서 강경 판정 | 오염을 인지하고 코드 심사로 걸러낼 의도가 명확하다 |

즉 **경쟁 상대는 리더보드가 아니라 "보이지 않는 규정 준수 제출물 집합"** 이다.
현재 우리 image-only 0.2522 가 그 안에서 어디인지는 알 수 없다.

## 4. 공개 E2E 모델 활용 가능성 — 코드 기준 판정

### SparseDrive (공식 repo + 공식 stage1 가중치 로컬 보유)

| 규정 항목 | 구현 | 판정 |
|---|---|---|
| 영상이 planner 에 기여 | ego feature = `ego_feature_encoder(feature_map)` (이미지 유래) | 준수 |
| ego status | **네트워크 예측값** `plan_status`; GT `ego_status` 는 학습 loss 타깃 (`motion_planning_head.py:449`) | **질문10 허용** |
| command | `decoder.py:170-174` 에서 `plan_cls[bs_indices, cmd]` — **출력 선택에만** | **질문8 허용** |
| goal/target point | 사용하지 않음 | 해당 없음 |
| 과거 frame feature 전달 | instance queue, `queue_length=4` | **질문4 답변 1/2 허용** |

**개조 없이 규정을 만족한다.** VAD 베이스라인이 `ego_his_encoder`/`ego_lcf_feat_idx`
를 끄면 준수인 것과 같은 구조이며, SparseDrive 는 그것이 기본 설계다.

### DiffusionDrive

SparseDrive 기반(`projects/configs/sparsedrive_configs` 포함). 동일 규정 프로필.
nuScenes L2 avg **0.57** (SparseDrive-S 0.61), 공식 가중치 HuggingFace 공개.

### VADv2

로컬 `src/VAD/VADv2` 는 config 1개 + head 1개뿐인 부분 이식이다. 공식 전체 구현·
가중치는 공개되지 않았다. 즉시 활용 대상이 아니다.

### 결정적 장벽 — 누적 T_infer

질문4 답변 4: T_infer 는 **초기화 이후 과거 시점 처리부터 마지막 궤적 출력까지
모든 model forward 의 누적**이다. streaming 모델은 프레임 수만큼 곱해 낸다.

| 모델 | 구성 | 보고/실측 | ETRI 누적 환산 | penalty |
|---|---|---|---|---:|
| SparseDrive-S | R50 704×256 ×6cam, queue 4 | 9.0 FPS ≈ 111ms/frame (README, 환경 미기재) | 약 444ms | ×2.72 |
| dense VAD 7-frame | — | **586.6ms (우리 3090 실측)** | 586.6ms | ×3.43 |
| **우리 T4 + learned selector** | R34 768×432 ×6cam ×4frame | **46.997ms (3090 실측)** | 46.997ms | **×1.000** |

SparseDrive 를 그대로 쓰면 L2 가 0.20 이어도 실효 0.54 가 된다.
**약 47ms 라는 지연은 우리가 직접 만들어서 얻은 유일하고 대체 불가능한 자산이다.**

(SparseDrive FPS 는 README 수치이고 측정 환경이 기재되지 않았다. 실측 전에는 추정이다.)

## 5. 권고

1. **리더보드 순위를 목표 함수로 쓰지 않는다.** 0.09 는 정당한 영상 모델의 범위가
   아니다. 목표는 "penalty ×1.000 을 유지한 최선의 규정 준수 점수" 로 재설정한다.
2. **공개 모델은 대체가 아니라 부품 공급원으로 쓴다.** SparseDrive/DiffusionDrive 의
   sparse perception(det/map/motion 공동 학습) 표현은 우리 scorer 의 약점(좁은 대역
   순위상관 0.40)을 직접 겨냥한다. 전체 교체가 아니라 **backbone/perception 초기값과
   보조 supervision 으로 이식**하는 것이 지연 예산과 양립한다.
3. **직접 만드는 것은 의미가 있다.** 다만 근거는 "성능 우위" 가 아니라
   (a) 47ms 지연, (b) 운영국이 권고한 두 형태 중 하나(후보 생성 + goal 선택)를 이미
   구현·검증했다는 점, (c) 어떤 공개 모델도 이 지연 예산 안에 들어오지 않는다는 점이다.
4. **측정해야 할 것**: SparseDrive-S 의 실제 3090 누적 지연. README FPS 만으로
   기각하지 않는다. 규정 준수 상태로 얼마나 빠른지가 이식 범위를 정한다.

## 6. 부수 확인

우리 A0 bank 의 완벽랭킹 상한 0.0996 이 리더보드 1위 0.0924 와 거의 같다.
계층 bank K14,279 는 0.0554 까지 내려간다(⑥-C). 즉 **후보 공간은 제약이 아니고,
제약은 여전히 영상 랭킹 품질**이라는 기존 결론이 유지된다.
