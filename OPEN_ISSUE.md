지금까지 ISSUE 정리.

## 질문1

모델 및 추론 코드를 구현하는 과정에서 테스트 데이터의 입력 활용 범위와 관련하여 몇 가지 확인이 필요해 문의드립니다.

1. **테스트 데이터의 `ego_pose.parquet` 활용 범위**
    
    테스트 데이터에 포함되어 제공되는 `ego_pose.parquet`의 각 row 및 column을 모델의 추론 입력으로 자유롭게 사용할 수 있는지 확인 부탁드립니다.
    
    특히 현재 시점 이후의 양의 frame index에 해당하는 pose 정보가 포함되어 있는 경우, 해당 정보의 **x, y 및 yaw(rpy) 값을 목적지/goal 정보로 사용하여 미래 Ego trajectory를 생성하는 것이 허용되는지** 궁금합니다.
    
2. **`command.parquet` 활용 여부**
    
    테스트 데이터에 제공되는 `command` 및 `vad_cmd` 정보를 모델의 입력으로 사용하는 것이 허용되는지 확인 부탁드립니다.
    
3. **E2E 모델의 카메라 입력 사용 조건**
    
    E2E Driving 과제가 멀티카메라 기반으로 정의되어 있는데, 최종 제출 모델이 반드시 카메라 영상에 함수적으로 의존해야 하는지 궁금합니다.
    
    즉, 제공된 테스트 입력 중 Ego pose, command 등의 정보를 함께 활용하는 것은 가능하되 카메라 정보의 사용이 필수 조건인지, 혹은 제공된 입력 데이터 범위 안에서 참가자가 입력 구성을 자유롭게 결정할 수 있는지 확인 부탁드립니다.
    
4. **추론 시간 측정 범위**
    
    안내서에는 NVIDIA RTX 4090 기준 단일 Scene당 추론시간(Tinfer)을 측정한다고 되어 있는데, 다음 항목들이 Tinfer에 포함되는지도 확인 부탁드립니다.
    
    - 이미지 JPEG decoding
    - resize / undistortion 등의 전처리
    - CPU→GPU 데이터 전송
    - 모델 forward
    - trajectory 후처리

**→ 답변**

문의하신 내용 아래와 같이 답변 드립니다.

**1. 테스트 데이터의 `ego_pose.parquet` 활용 범위**테스트 데이터에는 현재 시점 이후 5s(50프레임)의 좌표만 포함되어 있습니다. 그 사이 값은 비공개됩니다.추가로, 사전 안내한 내용처럼 테스트 데이터의 미래 5s 좌표는 활용 가능하나, 과거 정보와 목적지 기반 라벨링이나 역추적 행위를 통한 학습 활용은 불가능합니다.정리하면, 모델의 입력으로서 과거와 현재 그리고 목적지를 위한 미래 좌표 한 점(5s)을 사용하는 범위까지 허용됩니다.**2. `command.parquet` 활용 여부**두 command 모두 사용 가능합니다.**3. E2E 모델의 카메라 입력 사용 조건**제공되는 6개 카메라 중 일부만 사용하는 범위는 가능합니다.단, 이번 챌린지 목적에 맞춰서 Ego pose 및 command 정보만 활용하는 경우는 금지됩니다.**4. 추론 시간 측정 범위**

추론 시간의 경우 모두 동일한 환경에서 측정할 예정이며, 말씀하신 항목 중 데이터 전처리 항목과 후처리 항목은 포함되지 않습니다.즉, 모델 forward 계산 속도까지만 측정합니다.추가로, 모델의 결과인 trajectory에서 제출을 위한 후처리는 허용되나 위치 보정 등의 작업은 불가합니다.

## 질문2(공지1)

안녕하십니까? 챌린지 운영국입니다.
E2E 챌린지 추론 규정과 관련하여 내용을 정리하여 전달드립니다.

[공지] 추론 규정 안내 - 과거/미래 정보 활용 범위 및 영상 입력 의무

1. 데이터 제공 방침
본 챌린지는 참가자의 개발 편의를 위해, 자차의 과거 주행 정보와 일부 미래 시점 위치 정보를 제한을 둔 범위 내에서 최대한 공개하고 있습니다.
이는 모델 학습/검증/디버깅을 지원하기 위한 것으로, 추론(inference) 단계의 입력으로 사용하는 것을 허용한다는 의미가 아닙니다.
2. 챌린지의 목적
본 챌린지는 공개 데이터를 기반으로 카메라 영상 입력에서 주행 궤적을 산출하는 End-to-End 자율주행 모델을 개발하고, 관련 기술의 확대와 자율주행 기술 활성화를 촉진하는 데 목적이 있습니다.
따라서 평가 대상은 영상 인식에 기반한 주행 판단 능력이며, 자차 운동 상태의 연장선을 계산하는 능력이 아닙니다.
3. 금지 행위
다음 행위는 챌린지 목적에 부합하지 않으므로 금지합니다.

[공통 원칙] 본 항의 금지 여부는 구현 형태가 아니라 성능의 실질적 출처를 기준으로 판단합니다.
수식/규칙 기반 계산으로 궤적을 산출하는 경우는 물론, 동일한 계산을 학습을 통해 모델 내부에 반영하거나 신경망 구조로 대체하여 우회하는 경우에도 동일하게 금지됩니다.
즉, 자차의 과거 상태와 목표 지점만으로 궤적이 결정되는 구성은 신경망의 형태를 취하더라도 영상 기반 End-to-End 모델의 개발로 인정되지 않으며, 본 챌린지의 평가 대상이 아닙니다.

최종 궤적은 영상 인식 결과로부터 도출되어야 하며, 과거 상태와 목표 지점은 이를 보조하는 입력에 한합니다.

3-1. 미래 시점 정보의 활용
제공되는 미래 목표 지점(target point)은 내비게이션 목적지와 동일한 성격의 조건 입력(goal conditioning) 으로 모델에 제공할 수 있습니다. "어디로 향해야 하는가"를 모델에 알려주는 용도이며, 이는 허용됩니다.
다만 다음은 금지합니다.

- 영상 입력을 사용하지 않거나, 영상이 결과에 실질적으로 기여하지 않는 상태에서 목표 지점만으로 궤적을 생성하는 행위
- 다른 클립/시나리오와의 대조를 통해 제공 범위를 넘어서는 미래 구간을 복원/역추적하는 행위
즉, 목표 지점은 모델이 참조하는 지시 정보여야 하며, 궤적을 도출하는 계산의 근거가 되어서는 안 됩니다.

3-2. 과거 정보만을 이용한 운동학적 추론
과거 자차 궤적으로부터 속도/가속도 등 운동 상태를 산출하고, 등속/등가속/다항 회귀 등 운동학 기반으로 미래 궤적을 생성/산출하는 행위
즉, 특정 프레임의 자차 정보만 사용하고 같은 프레임의 영상은 사용하지 않는 구성은 허용되지 않습니다.

3-3. 규칙 기반 계산에 의한 궤적 산출
과거 정보를 입력으로 하되, 영상 인식을 거치지 않는 단순 계산/수식/규칙 기반 알고리즘으로 최종 궤적을 산출하는 행위
신경망 형태를 사용하더라도 영상이 결과에 실질적으로 기여하지 않는 경우 동일하게 간주합니다.

1. 영상 입력 의무 규정
공정성 확보를 위해 다음을 규정합니다.
과거 정보를 활용하는 모든 시점(프레임)에 대하여, 해당 프레임의 영상 정보가 반드시 모델의 입력으로 함께 사용되어야 하며, 최종 궤적은 해당 영상 입력을 소비하는 모델의 추론 결과여야 합니다.
2. 위반 시 조치
위 사항의 위반이 확인된 제출물은 순위에서 제외되며, 사안의 경중에 따라 팀 실격 처리될 수 있습니다.

## 질문3

안녕하세요. 과제3 E2E Driving 평가 지표 계산 방식에 대해 문의드립니다.

참여 가이드라인 PDF 13페이지에는 Planning L2가 미래 궤적 6개 지점 전체의 단순 평균으로 기재되어 있습니다.

반면 사전설명회 자료 44페이지에는 '각 시점(1/2/3초) L2 = 해당 구간 waypoint의 평균 변위(ADE)'이고 '최종 L2 score = 1·2·3초 L2의 평균'으로 되어 있어, 2단계로 계산하는 것으로 읽힙니다.

두 방식은 waypoint별 가중치가 달라집니다. 특히 '해당 구간'의 범위가 명확하지 않아 두 가지로 해석됩니다.

- 1초 L2 = 0.5초와 1.0초 waypoint의 평균 (누적 구간)
- 1초 L2 = 1.0초 waypoint 하나 (해당 시점만)

어느 방식이 실제 채점에 적용되는지 확인 부탁드립니다. 배포된 코드에 채점 스크립트가 포함되어 있지 않아 문의드립니다.

[답변]
사전설명회 자료가 맞으며 1/2/3초 l2의 평균 l2 avg가 최종 리더보드 대상입니다
1초면 2개 평균 2초면 4개 평균 3초면 6개 평균을 내고 그 3 개 지표의 다시 평균을 내서 l2 avg로 냅니다.

## 질문4

안녕하세요. E2E Driving 부문에서 이전 프레임의 BEV Feature를 다음 프레임으로 전달하는 모델의 과거 정보 사용 범위와 추론 시간 측정 기준을 문의드립니다.

운영팀에서 배포한 베이스라인 모델인 VAD를 기준으로 확인해 보면, tools/data_converter/etri_test_converter.py에서는 테스트 데이터로 제공되는 31프레임 중 frame=-30,-25,-20,-15,-10,-5,0에 해당하는 7프레임을 생성합니다. 이후 tools/etri_test_submit.py에서는 test set 추론 시 각 sequence의 첫 프레임에서 reset_stream(model.module)을 호출해 prev_bev를 None으로 초기화하고, 위 7프레임을 시간순으로 모델에 입력합니다.

reset_stream(model.module)  # 최초 prev_bev=None

for gi in sample_ids:
result = model(return_loss=False, rescale=True, **collated)

# 마지막 프레임의 결과만 제출

ego_fut_preds = result[0]["pts_bbox"]["ego_fut_preds"]

또한 projects/mmdet3d_plugin/VAD/VAD.py에서는 현재 프레임에서 계산된 new_prev_bev를 저장한 뒤, 다음 프레임의 prev_bev로 전달합니다.

new_prev_bev, bbox_results = self.simple_test(
...
prev_bev=self.prev_frame_info["prev_bev"],
)

self.prev_frame_info["prev_bev"] = new_prev_bev

즉, 배포된 VAD는 마지막 프레임만 prev_bev=None으로 독립적으로 추론하는 방식이 아니라, 앞선 6프레임을 먼저 추론하면서 생성된 BEV Feature를 순차적으로 전달하고 마지막 프레임의 궤적만 제출하는 구조로 확인됩니다.

이전 프레임의 BEV Feature 사용 여부는 모델 성능에도 직접적인 영향을 줄 수 있습니다. 내부적으로 Validation을 진행했을 때에도 과거 프레임을 순차적으로 처리하면서 BEV Feature를 전달한 경우와 마지막 프레임만 prev_bev=None으로 추론한 경우 사이에 상당한 성능 차이가 있었습니다. 따라서 이전 프레임에서 생성된 feature 또는 latent를 마지막 프레임 추론에 사용하는 것이 허용되는지 확인 부탁드립니다.

추론 시간 측정 기준에 대해서도 문의드립니다. 공식 tools/measure_flops.py는 한 프레임을 먼저 실행해 prev_bev를 생성한 다음, prev_bev가 존재하는 다음 한 프레임의 연산량을 측정합니다. 배포 가이드에는 1차 탈락 기준이 단일 프레임 연산량 약 7053 GFLOPs를 초과하는 경우로 안내되어 있습니다.

Error Score에 반영되는 T_infer가 이전 프레임에서 생성된 prev_bev가 준비된 상태에서 마지막 프레임을 한 번 추론하는 데 걸리는 시간인지 궁금합니다. 이 경우 앞선 프레임들을 처리하여 prev_bev를 생성하는 데 걸린 시간은 제외되고, 마지막 프레임의 단일 추론 시간만 T_infer에 반영되는 것으로 이해하면 되는지 확인 부탁드립니다.

만약 마지막 프레임의 추론 시간만 측정하는 것이 아니라면, prev_bev=None으로 초기화한 후 앞선 프레임들을 처리하고 마지막 궤적을 출력할 때까지의 전체 시간을 측정하는지, 또는 처리한 프레임들의 평균 추론 시간을 측정하는지도 안내 부탁드립니다.

정리하자면, 다음 네 가지를 확인하고 싶습니다.

1. 마지막 프레임을 반드시 prev_bev=None으로 추론해야 하는지, 아니면 배포된 etri_test_submit.py와 같이 앞선 프레임들을 먼저 추론하고 여기서 생성된 BEV Feature 또는 latent를 마지막 프레임에 전달하는 방식이 허용되는지 문의드립니다.
2. 이러한 방식이 허용될 경우, 배포된 VAD뿐만 아니라 이전 프레임의 feature 또는 latent를 다음 프레임으로 전달하는 다른 모델에도 동일하게 적용되는지 문의드립니다.
3. 이와 같이 이전 프레임의 feature를 다음 프레임으로 전달하는 경우, 베이스라인(VAD)과 동일하게 7프레임만 사용할 수 있는지, 그리고 사용할 수 있는 과거 프레임의 범위나 sampling 간격에 별도의 제한이 있는지 문의드립니다.
4. 이전 프레임의 feature 또는 latent를 마지막 프레임에 전달하는 경우, T_infer에는 마지막 프레임의 단일 추론 시간만 반영되는지, 아니면 해당 feature 또는 latent를 생성하기 위해 앞선 프레임들을 처리한 시간까지 포함되는지 문의드립니다.

[답변]
1. 배포 베이스라인처럼 앞선 프레임을 순차 처리해 생성된 bev feature 등을 전달하는 방식은 허용됩니다.
2. 베이스라인에 한정되지 않으며, 이전 프레임의 feature를 다음 프레임으로 전달하는 등의 여러 구조 등은 모든 모델에 동일하게 허용됩니다.
3. 사용 가능한 범위는 제공되는 과거 3s 범위로 한정되며, 그 안에서 프레임 수 및 샘플링 간격은 자유입니다. 단, 과거 사용 시 영상은 필수로 같이 사용되어야 합니다.
4. t_infer의 경우 마지막 프레임이 아니라 초기화 이후 과거 시점 처리부터 마지막 궤적 출력까지 수행되는 모든 model forward의 누적 시간으로 측정됩니다.

## 질문5

안녕하십니까. 차에서추운곳은차가운데 최원준 입니다.

공지해주신 E2E Driving 금지 행위와 관련하여 아래 행위도 금지 행위에 포함되는지 궁금하여 문의 드립니다.

1. 영상 데이터를 신경망에 입력하여 궤적을 추론한 이후, 신경망에 입력되지 않은 과거/현재 정보를 이용한 별도의 계산/수식/규칙 기반 후처리(post-processing)을 적용하여 최종 궤적을 산출하는 경우도 금지행위에 해당하는지 궁금합니다.
2. 만약 위와 같은 post-processing이 허용된다면 T_Infer 측정 기준이 궁금합니다.
예를 들어 이전 프레임의 post-processing과 현재 프레임의 GPU 신경망 inference가 pipeline 형태로 병렬 수행되는 경우, T_Infer를 두 작업의 처리 시간 합으로 측정하는지, 아니면 병렬 수행을 고려한 실제 최종 궤적 출력까지의 경과 시간을 기준으로 측정하는지 확인 부탁드립니다.

감사합니다

1. 신경망에 입력되지 않은 정보를 활용한 후처리는 금지됩니다. 다만 모델 출력을 제출 규격으로 변환하기 위한 후처리는 허용됩니다.
2. model forward만 계산하며 해당 후처리는 금지됩니다. 다만 규격 변환을 위한 후처리는 측정되지않습니다.

## 질문6

Question 1. In the configuration above — where images come only from the current frame, and only past ego_pose values are referenced to compute speed — does this count as "using the past" under item 3, such that past images must also be provided as input?
Speed and yaw rate cannot be derived from a single pose; they require differencing two timestamps. We understand the provided baseline likewise obtains vx/vy in gt_ego_lcf_feat from pose data. If the intent of item 3 is to prevent pose-only models that ignore imagery, our configuration would appear to be within scope — but read literally, past images would be mandatory.
Question 2. Is there any restriction on using the 5-second target point (frame=+50) included in each test clip's ego_pose.parquet? Item 3 appears to govern the past 3 seconds, whereas the target point is future information that is nonetheless provided at test time, so we would like to confirm it separately.

1. 과거 pose를 이용해 현재 ego status를 구하는 용도는 허용되며, 이를 계산하는데 사용된 과거 영상까지 입력할 필요는 없습니다. 베이스라인 예시를 참고해주세요.
2. 미래 좌표는 모델 입력 condition으로 활용할 수 있으며, 진행 방향이나 후보 궤적 선택 등에 영향을 주는 것은 허용됩니다. 다만 target point와 자차 상태만으로 최종 궤적을 생성하거나 영상 인식과 무관하게 경로를 결정하는 방식은 허용되지 않습니다.

## 질문7

안녕하세요. 3번 E2E 부문에서 과거 자차 궤적 정보 활용 범위와 영상의 실질적 기여 판정 기준에 대해 문의드립니다.

저희 팀은 추론 규정 공지에 따라 과거 정보를 활용하면서 동일한 프레임의 영상을 입력으로 하는 E2E 모델을 개발해왔습니다.

다만 자체 검증(validation에서 영상 입력을 단색 프레임으로 교체해 L2를 비교) 결과, 다음을 확인했습니다.

- 기존 구성한 모델 : 영상 + command + target point: 영상 제거 시 L2 약 1.4배 악화 → 영상이 결과에 기여
- 위 모델 + ego_pose로 부터 ego his encoder, ego_lcf_feat_idx를 추가해 학습한 모델: 영상 제거 시 L2 변화 없음

즉, 구성상 기존 영상 기반 E2E 모델에서 과거 궤적 관련 정보만을 추가해 학습했을 때, 결과적으로 모델이 과거 궤적에 의존하게 되는 경우가 발생함을 확인했습니다.

기존 공지에서 말씀 주신 'target point 활용에 따른 영상이 실제 결과에 기여하지 않는 상태에서 궤적 생성' / '특정 프레임의 자차 정보만 사용하고 같은 프레임의 영상은 사용하지 않는 구성'이 아님에도 학습 결과로 규정의 취지에 맞지 않는 모델이 만들어진 것으로 판단되어 아래를 여쭙습니다.

[질문]

1. 실질적 기여의 판정 기준: 심사 시 '영상이 결과에 실질적으로 기여한다'를 어떤 방법과 기준으로 확인하실 지 구체적으로 알려주실 수 있을까요? 현재는 영상 입력을 0으로 할 때의 L2 변화를 기준으로 확인하고 있는데, 영상 기여 판단에 대한 구체적인 기준이 더 있으면 참고하기 좋을 것 같습니다.
2. 자차 상태·과거 궤적의 허용 범위: 사전설명회에서 hd_ego_pose 활용 가능성을 말씀해 주셨고, 공지에서도 과거 pose로 현재 자차 상태를 구하는 것을 허용하신 것으로 이해했습니다. 혹시 자차 상태·과거 궤적을 네트워크 입력으로 사용하는 건 허용되는 것인지 확인 부탁드립니다.
허용된다면, 이처럼 자차 상태/과거 궤적이 입력이어도 영상의 실질 기여가 되는 방식으로 학습한다면 제한이 없는 것인지, 그 기준이 궁금합니다.
현재 저희의 경우, 기존 개발하던 모델에서 과거 궤적을 입력으로 추가 하여 학습을 한 것만으로 영상의 실질 기여가 줄어든 상황인데 이 경우에도 제한이 되는 것인지 여쭙고 싶습니다.

바쁘실 와중에 살펴봐주셔서 감사합니다.

[답변]
1. 판정 기준은 과거 궤적이나 status가 planner에 직접 또는 단순 임베딩으로 입력되어 최종궤적을 출력하는 여부에 결정됩니다. 배포 베이스라인 기준 과거 traj과 status를 planner에 입력으로 쓰는 구조를 지원하나 기본적으로 사용하지 않습니다. 이를 활성화 시 과거 궤적과 status는 원본 및 임베딩 값으로 scene feature와 함께 planner에 직접 들어가 scene feature가 포함되어도 과거 값이 매우 크게 동작하여 최종 궤적이 생성될 수 있습니다. 이 경우 신경망 형태를 사용하더라도 영상이 결과에 실질적으로 기여하지 않는 것으로 판단합니다.
따라서 과거 정보를 직접적으로 planner 입력으로 또는 단순 임베딩 형태로 사용하는 것을 금지하며, 여러 task의 공통 특징을 향상하는 등의 간접적 활용은 허용합니다.
2. 정확한 판단은 코드를 기반으로 판단 가능하며 위와 같은 형태로 추론한 결과로 판단됩니다.


## 질문 8
E2E 부문 target point 활용 방식에 대해 문의드립니다.

기존 답변에서

1. 미래 좌표를 모델 입력 condition으로 활용 가능하다고 하셨고, 2. 과거 정보를 planner에 직접/단순 임베딩으로 입력하는 것은 금지, 간접적 활용은 허용이라고 확인해주셨습니다.

target point는 과거 정보가 아닌 제공된 미래 condition인데, 이 경우에도 2.의 기준이 동일하게 적용되는지 궁금합니다.
구체적으로:
target point를 planner(궤적 디코더)에 직접 입력하는 것은 허용되는지, 아니면 과거 정보와 동일하게 금지인지
target point를 BEV feature 생성 단계의 condition으로 주입하여 (planner 직접 입력이 아닌) scene 특징 형성에 간접적으로 활용하는 것은 허용되는지

감사합니다.

[답변]
미래도 동일합니다. 다만 이전 답변들처럼 궤적 선택 등 궤적 생성에 기여나 알고리즘 등의 기여가 아니라 모델의 여러 출력 중 선택에만 활용되는 경우는 허용됩니다.

[한 번 더 확인]
답변 감사합니다. 앞서 "미래도 동일"이라고 답변해주셨는데, 과거 정보 기준에는 "간접적 활용(여러 task의 공통 특징을 향상하는 등)은 허용"이 포함되어 있었습니다. target point도 동일하게, planner 입력이 아닌 BEV/scene feature 형성 단계의 조건 입력으로 사용하는 간접적 활용은 허용되는지 확인 부탁드립니다.

[답변]
네,이해하신 내용이 맞습니다.

## 질문9(공지2)

챌린지 운영국입니다.
E2E Driving 평가 관련 공지 드립니다.

기 공지된 사항과 오픈채팅 답변을 통해 Goal, ego status, 과거 정보 활용 및 추론시간 기준은 충분히 안내되었고 판단 기준이 되는 취지 또한 명확히 전달드렸습니다.
모든 구조와 방식을 개별적으로 규정하지는 않습니다. 이미 답변된 사안이거나 규정 취지에 어긋날 가능성이 있다고 판단되는 방식이라면 시도하지 마시기 바랍니다.
규정의 취지를 확대 해석하여 우회하는 방식은 인정하지 않으며, 모든 개별 상황을 사전에 구체적으로 규정해 드릴 수는 없습니다.

최종 허용 여부는 수상 후보 심사 시 제출 코드를 기준으로 판정하며, 판단 기준이 되는 베이스라인 코드도 이미 제공되어 있음을 다시 한번 안내해 드립니다.
추론시간은 모든 팀에 대해 동일한 기준(동일 환경)으로 측정됩니다.

운영국에서는 결과물에 대한 최종 평가 시 규정의 취지를 확대 해석하여 우회하는 방식으로 제출한 결과물 여부에 다시 한번 더 판단 후 탈락 여부를 결정할 예정입니다
또한 운영국에서 평가하는 최종 결과에 대해서는 "이의제기"는 받지 않을 예정이며, 이미 참가 신청서의 서약서에 명시 및 안내된 내용입니다.
이점 명심하시고 챌린지 진행하여 주시기 바랍니다.

향후 문의는 위 원칙만으로 판단하기 어려운 구체적이고 개별적인 사안에 한해 주시기 바랍니다.


## 중요(우리 작업 내용 기반 오픈채팅 질문)
  8/31 오픈채팅으로 안내해 주신 판정 기준 — "과거/미래 정보를 planner에 직접 또는 단순 임베딩 형태로 입력하는 것은 금지, 여러 task의 공통 특징을 향상하는 간접적 활용과 모델의 여러 출력 중 선택에만 활용하는 것은
  허용" — 을 저희 모델에 적용해 자체 점검하던 중, 저희 구조가 어느 쪽에 해당하는지 코드 수준에서 확인이 필요하여 문의드립니다.
                                                                                                          
  [저희 궤적 디코더 구조]                                                                                 
                                                                                                          
  1. 최종 궤적은 cross-attention의 출력으로 생성되며, attention의 value(값)는 해당 시점 카메라 영상에서 유도된 feature만으로 구성됩니다. goal과 command는 value에 포함되지 않습니다.
  2. 제공되는 미래 목표점(goal 좌표)과 command는 attention의 query 구성에만 사용됩니다. 즉 "영상 증거의 어느 부분을 읽을지"를 지시하는 주소 정보로만 관여하며, 궤적 좌표를 계산하는 값 경로에는 들어가지 않습니다.
  3. 이 분리가 학습 중에도 깨지지 않도록 value 경로의 bias/affine 항을 모두 제거해 구조적으로 보장했습니다. 그 결과 카메라 영상 입력이 0이면 goal·command가 어떤 값이든 출력 궤적이 정확히 0이 됩니다(근사값이 아닌
     exact zero이며, 단위테스트로 상시 검증하고 있습니다). 즉 goal만으로는 어떠한 궤적도 생성될 수 없고, 궤적 값의 유일한 출처는 영상 인식 결과입니다.
  4. ego 속도·가속도·yaw rate·과거 궤적은 학습·추론 전 과정에서 네트워크에 입력하지 않습니다(베이스라인의 ego_his_encoder, ego_lcf_feat_idx, can_bus 임베딩 모두 미사용). 과거 프레임 BEV feature의 기하
     정렬(shift/rotate)에는 배포 베이스라인과 동일하게 시점 간 상대 pose만 사용하며, 과거 정보를 쓰는 모든 프레임의 영상을 함께 입력합니다(공지 4조 준수).
                                                                                                          
  [질문]                                                                                                  
                                                                                                          
  Q1. 위 구조처럼 goal이 (a) 궤적 값을 만드는 경로에서 구조적으로 차단되어 있고, (b) 영상 증거를 읽을 위치 지정(attention query)에만 사용되며, (c) 영상이 없으면 출력이 정확히 0인 경우 — 8/18 공지 3-1항의 "어디로
  향해야 하는가를 알려주는 참조 지시 정보"로서 허용 범위에 해당하는지요? 아니면 8/31 답변의 "planner에 직접 또는 단순 임베딩 형태로 입력"에 해당하여 금지인지요?
                                                                                                          
  Q2. 만약 Q1이 금지에 해당한다면, 저희는 영상만으로 복수 후보 궤적을 생성하고 goal은 그중 하나를 선택하는 데에만 사용하는 방식(안내해 주신 "모델의 여러 출력 중 선택에만 활용")으로 전환할 예정입니다. 이 전환
  방식이 허용 범위에 해당한다는 이해가 맞는지 함께 확인 부탁드립니다.                                     
                                                                                                          
  수상 후보 심사 시 필요하시면 해당 모듈 코드와 영상-제로 검증 테스트를 즉시 제출하겠습니다. 감사합니다.  

  [답변]
  구성하신 구조와 의도는 명확히 이해되며, 합리적으로 설계된 것으로 판단됩니다.

    다만 사전 안내드린 기준 및 타 팀과의 형평성을 고려하여 해당 구조는 허용 범위로 보기 어렵습니다. goal이 cross attention의 query항으로 planner 입력에 포함되어 동작하기 때문입니다. value인 영상이 제거되어 출력이 0이 되는 것은 맞으나, 이는 attention 연산의 특성에 따른 값일 수 있으며 goal이 traj을 생성하는 planner 내부에 입력되어 궤적 생성에 관여합니다.

    따라서 goal은 planner의 경로가 아닌 인식이나 공통 feature 등의 향상 단계에서 활용하시거나 후보 궤적 중에서 선택하는 방식으로 활용하시길 권장드립니다.

## 질문 10
[E2E Driving]
안녕하세요. VAI 이민승입니다.
E2E Driving 관련해서 문의드립니다.

Ego history trajectory나 ego status를 데이터로부터 입력 받아서 사용하는 것이 아닌, 영상으로부터 history, status를 네트워크가 직접 추론하고 이를 planning에 사용하는 것이 허용되는지 질문드립니다.

[답변]
허용됩니다. Test 데이터로부터 ego history 및 status를 입력받는 것이 아니라, 네트워크가 영상으로부터 직접 추론한 결과를 planner에 사용하는 것은 해당 값이 영상에서 파생된 정보이므로 허용됩니다.